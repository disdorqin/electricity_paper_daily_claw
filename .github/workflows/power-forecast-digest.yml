name: Power Forecast Digest

on:
  workflow_dispatch:
  schedule:
    # 09:00 Asia/Shanghai = 01:00 UTC
    - cron: "0 1 * * *"

permissions:
  contents: read

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Send digest to WeCom
        env:
          # Preferred name
          WECHAT_WEBHOOK: ${{ secrets.WECHAT_WEBHOOK }}
          # Compatible aliases. GitHub returns empty string when a secret does not exist.
          WECOM_WEBHOOK: ${{ secrets.WECOM_WEBHOOK }}
          WECHAT_BOT_WEBHOOK: ${{ secrets.WECHAT_BOT_WEBHOOK }}
          WECHAT_ROBOT_WEBHOOK: ${{ secrets.WECHAT_ROBOT_WEBHOOK }}
          QYWX_WEBHOOK: ${{ secrets.QYWX_WEBHOOK }}
          ENTERPRISE_WECHAT_WEBHOOK: ${{ secrets.ENTERPRISE_WECHAT_WEBHOOK }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ secrets.OPENAI_MODEL || 'gpt-4o-mini' }}
          GITHUB_TOKEN: ${{ github.token }}
        run: python scripts/power_forecast_digest.py
